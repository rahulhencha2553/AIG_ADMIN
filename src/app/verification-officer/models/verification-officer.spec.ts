import { VerificationOfficer } from './verification-officer';

describe('VerificationOfficer', () => {
  it('should create an instance', () => {
    expect(new VerificationOfficer()).toBeTruthy();
  });
});
