import { VerificationPortalOfficer } from './verification-portal-officer';

describe('VerificationPortalOfficer', () => {
  it('should create an instance', () => {
    expect(new VerificationPortalOfficer()).toBeTruthy();
  });
});
