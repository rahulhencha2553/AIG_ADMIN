import { AddressVerification } from './address-verification';

describe('AddressVerification', () => {
  it('should create an instance', () => {
    expect(new AddressVerification()).toBeTruthy();
  });
});
